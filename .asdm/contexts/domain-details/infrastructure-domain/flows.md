# 基础设施域 - 核心业务流程

## LLM 路由流程
1. 收到 LLM 请求
2. 按优先级选择 Provider
3. 检查 Provider 可用性
4. 发送请求
5. 如果失败 → 自动切换下一个 Provider
6. 重试最多 3 次
7. 返回结果或错误

## 规则引擎执行流程
1. 加载规则文件 (YAML/JSON)
2. 编译正则表达式
3. 收到输入文本
4. 逐条规则匹配 (关键词 + 正则)
5. 收集匹配结果
6. 按严重度排序
7. 返回匹配列表
